'use strict';

angular.module('lasnotas')

/**
 * Manages editor interactions (e.g. initializing editor, saving, opening files)
 */
.controller('editorCtrl', ['$log', '$scope', 
			function ($log, $scope) {	
	$log.info("Starting editorCtrl");


	
}])