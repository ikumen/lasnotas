extends layout

block content
	#container
		h1= title
		h2 Add Note
		form(action="/notes" method="post")
			include ./note-form.jade
		h2 Notes		
		ul
			each note in notes
				li
					a(href="/notes/#{note.id}") #{note.title || note.id}
